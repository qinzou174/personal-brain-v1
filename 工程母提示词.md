# Personal Brain V1

# 统一个人数字记忆与 AI 上下文基础设施

## Engineering Master Prompt

***

# 0. 你的角色

你现在负责设计、开发、测试、部署并交付一套长期运行的：

# Personal Brain

这不是一个普通 RAG 知识库。

也不是给 TRAE、ChatGPT、Cursor 分别做三个知识库。

它是用户**唯一、统一、长期存在的个人数字记忆与数据基础设施**。

以后：

- TRAE CN
- Cursor
- ChatGPT
- 手机 IDE
- 桌面 Agent
- Telegram Bot
- 用户自己的 AI
- 未来任何支持 MCP/API 的 AI

都只是 Personal Brain 的不同客户端。

真正拥有长期记忆的是：

> 用户自己的家庭 Linux 服务器。

客户端可以换。

账号可以换。

模型可以换。

IDE 可以换。

聊天可以被压缩。

但 Personal Brain 中的数据、历史、知识、项目上下文和个人画像不能跟着消失。

***

# 1. 项目最终愿景

最终架构理念：

```
                     Personal Brain

                用户唯一长期数字大脑
                         │
       ┌─────────────────┼──────────────────┐
       │                 │                  │
       ▼                 ▼                  ▼

 Personal Knowledge   Project Brain       Personal Data

 日记                  项目背景             Expense
 工作文档              Modules              Todo
 笔记                  Tasks                Events
 旅游感想              Decisions            Collections
 哲学                  Constraints          Records
 Self Model            History
 审美                  Git Context
 偏好
 图片/音频/文件
       │                 │                  │
       └─────────────────┼──────────────────┘
                         │
                 Search / Context
                         │
                        MCP
                         │
        ┌────────────────┼────────────────┐
        ▼                ▼                ▼
      TRAE            ChatGPT           Cursor
        │                                  │
        ▼                                  ▼
   Windows PC                        Other Devices

                        +
                手机 / 未来 AI / Agent
```

禁止把系统设计成：

```
TRAE Knowledge DB

ChatGPT Knowledge DB

Cursor Knowledge DB
```

必须只有：

```
ONE PERSONAL BRAIN
```

***

# 2. 最核心原则

下面这些属于项目最高级原则。

后续实现不得违反。

## 2.1 一个 Brain，多客户端

Personal Brain 是服务器端唯一权威系统。

所有客户端访问同一份数据。

客户端不拥有独立长期知识库。

***

## 2.2 平台无关

Personal Brain Server 不应该知道：

```
“TRAE 是什么产品”
“Cursor 是什么产品”
“ChatGPT 是什么产品”
```

它只知道：

```
Client
Authentication
Scope
Tool
Request
```

任何未来支持 MCP/API 的客户端都能接入。

***

## 2.3 原始事实 > AI 摘要

AI 摘要不是事实本身。

例如：

```
原始日记
原始 PDF
原始图片
Git
Expense Record
```

属于原始事实。

而：

```
摘要
标签
Self Model
Embedding
AI推断
Daily Digest
```

属于派生数据。

任何派生数据必须可以重新生成。

原始数据不得因为 AI 已经总结过就被删除。

***

## 2.4 源码 = 当前事实

项目开发部分必须遵循：

```
源码 = 当前事实

Git = 历史事实

Project Brain = 项目理解

MCP = AI访问接口
```

知识库不能取代真实代码。

AI 真正修改某个模块之前仍然必须阅读当前相关源码。

Personal Brain 的作用是：

> 不再让 AI 每次从零重新理解整个项目。

***

## 2.5 能结构化的数据优先结构化

例如：

```
消费
Todo
Task
Project
Module
Decision
Event
```

应该存数据库。

不能把：

> “这个月花了多少钱？”

交给 RAG 猜。

应该使用准确 SQL 聚合。

***

## 2.6 推断永远不能冒充事实

Personal Brain 必须区分：

```
FACT
EXPLICIT USER STATEMENT
OBSERVATION
AI EXTRACTION
AI INFERENCE
```

AI 自己推测出来的信息必须带：

```
confidence
source
evidence
```

***

# 3. Personal Brain 管理的信息类型

V1 数据模型必须至少能够表达以下概念。

***

# 3.1 Knowledge

长期知识：

```
日记
笔记
工作文档
旅行感想
项目理念
个人哲学
学习资料
文章
说明文档
```

***

# 3.2 Asset

原始文件：

```
PDF
DOCX
TXT
Markdown
图片
音频
ZIP
其他附件
```

V1：

保存原始文件。

建立 Metadata。

支持基础解析。

不要求对所有复杂媒体进行深度 AI 理解。

***

# 3.3 Memory

长期可复用的信息。

例如：

```
过去发生的事情
历史经验
重要偏好
项目经验
某次重大决定
```

Memory 必须有生命周期。

***

# 3.4 Record

结构化记录。

V1 强制支持：

```
Expense
Todo
```

以后可扩：

```
Schedule
Subscription
Collection
Health Data
Habit
```

***

# 3.5 Event

表达：

> 某个时间发生了什么。

例如：

```
旅行
完成项目任务
发生一次消费
上传一篇日记
完成 Todo
产生重大 Decision
```

***

# 3.6 Entity

长期存在的对象。

数据模型至少预留：

```
Person
Place
Project
Concept
Media
Device
Organization
```

V1 不需要复杂知识图数据库。

使用 PostgreSQL 实现即可。

***

# 3.7 Relation

对象之间的关系。

例如：

```
Task MODIFIED Module

Decision AFFECTS Module

Expense BELONGS_TO Trip

Photo TAKEN_DURING Event

Preference SUPPORTED_BY Evidence

Diary DESCRIBES Event
```

V1 使用关系表实现。

暂时不引入 Neo4j。

***

# 3.8 Profile / Self Model

Personal Brain 必须能够逐渐形成用户长期画像。

包括：

```
Preferences
Aesthetics
Values
Working Style
Communication Style
Interests
Habits
Long-term Goals
Philosophy
```

但 AI 不得随意修改 Self Model。

详细规则见后文。

***

# 4. Self Model 的核心原则

Self Model 不是：

```
profile.txt
```

它必须是动态、有时间、有证据、有历史的。

例如：

```
Preference:
真实摄影感

Status:
active

Confidence:
0.91

Evidence:
#182
#201
#387

First Seen:
...

Last Confirmed:
...
```

***

# 5. Self Model 写入 A/B/C 规则

这是强制设计要求。

## A：用户明确要求记住

例如：

> 记住，我非常喜欢这种风格。

直接写入正式长期记忆。

```
status = active
source = explicit_user_statement
confidence = high
```

***

## B：默认模式

普通偏好、倾向、兴趣、行为模式：

不要立即变成长期画像。

例如：

> 最近感觉挺喜欢爵士乐。

保存为：

```
candidate_preference

confidence = low
evidence_count = 1
```

随着更多 Evidence：

逐渐提高 Confidence。

达到一定证据强度后：

```
candidate
→ established
```

这是系统默认行为。

***

## C：重要价值观 / 哲学 / 身份性长期原则变化

例如：

```
核心价值观
人生哲学
长期自我定义
重大关系定义
重大长期原则
```

不得自动正式修改。

必须先让用户确认。

确认以后：

更新长期 Self Model。

***

# 6. 用户明确陈述优先级最高

如果 AI 长期推断：

```
用户喜欢 X
```

而用户明确说：

```
其实我不喜欢 X。
```

必须：

```
旧推断 → corrected / superseded

新明确陈述 → active
```

系统不能拿行为分析反过来否定用户本人。

***

# 7. Self Model 必须允许变化

不能只记录：

```
用户喜欢 X
```

还要支持：

```
valid_from
valid_to
status
```

例如：

```
2025：
偏暖色

2026：
偏冷色真实摄影
```

旧偏好不删除。

标记：

```
historical
superseded
```

***

# 8. Self Model 必须允许矛盾

人不一定只有一个一致标签。

例如：

```
喜欢独处

同时

重视稳定关系
```

不能为了数据整洁强行消除。

支持：

```
context
exception
strength
evidence
```

***

# 9. Evidence 体系

所有重要 AI 推断必须能解释：

> 为什么认为这是用户偏好？

建立：

```
Evidence
```

例如：

```
Preference:
喜欢自然摄影

Evidence:
- 图片评价
- 日记
- 明确陈述
- 收藏行为
```

以后用户问：

> 你凭什么觉得我喜欢这个？

Personal Brain 必须能够回溯 Evidence。

***

# 10. Memory 生命周期

至少支持：

```
candidate
active
temporary
historical
superseded
expired
archived
deleted
```

不同对象有不同 Retention Policy。

禁止统一按：

```
“超过一年就失效”
```

处理。

***

# 11. 不同信息的保留策略

例如：

```
生日等明确事实
→ 长期

Expense
→ 永久事实

Project Decision
→ 永久历史

Todo
→ 完成后归档

Temporary State
→ 可自动过期

Candidate Preference
→ 可随证据衰减

Confirmed Philosophy
→ 除非用户修改，否则保持
```

***

# 12. 冲突处理

发现：

```
旧信息 A
新信息 B
```

不能偷偷删除其中一个。

建立：

```
conflict
supersedes
superseded_by
```

如果可以确定是时间变化：

自动历史化旧版本。

如果无法确定：

进入：

```
Inbox / Needs Review
```

只有真正需要使用冲突内容时，才优先做：

```
Just-in-time clarification
```

不要天天打扰用户整理数据库。

***

# 13. Personal Brain Intake

所有输入统一经过：

```
Intake
```

客户端来源可以包括：

```
ChatGPT
TRAE
Cursor
手机 AI
Telegram
Web
Manual Upload
Git
System
```

统一流程：

```
Input
 ↓
Intent / Type Detection
 ↓
Security Check
 ↓
Raw Save
 ↓
Structured Extraction
 ↓
Relation
 ↓
Index
 ↓
Optional Profile Update
```

***

# 14. Raw Input 必须保留

例如用户说：

```
今天去京都，午饭花了 4200 日元，
人太多，不过晚上特别舒服。
```

必须保存原文。

再派生：

```
Event

Expense

Place

Experience

Candidate Preference
```

所有派生对象：

```
derived_from = raw_input_id
```

以后更换 AI 模型，可以重新解释历史 Raw Input。

***

# 15. Intake 动作等级

实现：

```
L0 Ignore

L1 Auto Save

L2 Candidate Save

L3 Confirmation Required
```

## L0

例如：

```
哈哈
行
收到
```

无长期价值。

## L1

例如：

```
明确记账
明确 Todo
上传文件
项目变化
明确日记
```

自动保存。

## L2

例如：

```
偏好
兴趣
审美变化
行为趋势
```

候选保存。

## L3

例如：

```
核心哲学变化
重大 Self Model 变化
大范围删除
重大权限修改
```

确认后执行。

***

# 16. Expense

V1 必须实现。

例如：

```
午饭 38
打车 26
```

结构化存储：

```
amount
currency
category
description
occurred_at
source
event_id
```

例如用户问：

> 这个月花了多少钱？

必须：

```
SQL aggregate
```

而不是向量搜索。

***

# 17. Todo

V1 必须实现。

字段至少：

```
content
status
due_at
priority
created_at
completed_at
source
```

状态：

```
pending
in_progress
completed
cancelled
```

完成后归档。

不要直接删除。

***

# 18. Event / Timeline

Event 是 Personal Brain 的时间脊柱。

字段至少考虑：

```
event_id
type
title
description
start_time
end_time
importance
source
confidence
parent_event_id
```

支持：

```
大事件
 └── 子事件
```

例如：

```
日本旅行
 ├── 京都
 ├── 东京
 └── 大阪
```

***

# 19. 客观事实与主观体验分离

例如：

```
客观：
去了京都

主观：
觉得人太多
```

不能直接变成：

```
京都 = 不喜欢
```

主观体验必须带：

```
event
time
context
```

***

# 20. Asset 存储

V1 支持：

```
PDF
Word
Markdown
TXT
图片
音频
ZIP
```

原始文件不要直接塞 PostgreSQL。

使用文件系统 Storage Backend。

例如：

```
/opt/personal-brain/data/assets/
```

文件使用内容 Hash 管理：

```
SHA256
```

数据库保存：

```
asset_id
original_name
mime
size
sha256
storage_path
created_at
source
metadata
```

***

# 21. Storage 抽象

实现：

```
StorageBackend
```

V1：

```
LocalFilesystemBackend
```

未来允许：

```
NAS
S3
MinIO
Object Storage
```

业务逻辑不得绑定具体文件路径实现。

***

# 22. 图片

V1：

```
保存原图
Metadata
EXIF
时间
基础视觉描述
用户备注
Tags
```

如果 AI 猜地点：

只能作为：

```
candidate metadata
confidence < 1
```

如果 EXIF GPS 明确存在：

则可作为事实。

***

# 23. 音频

V1：

```
保存原音频
Metadata
时长
格式
用户备注
```

可选：

```
语音转文字
```

但复杂：

```
音乐审美自动演化分析
```

属于 V2。

***

# 24. ZIP

默认：

```
保存 Asset
读取基本文件列表
```

不要默认全部展开索引。

只有用户明确要求分析时：

```
安全解压
↓
限制文件数量
↓
限制体积
↓
Secret Filter
↓
Index
```

***

# 25. Secret Filtering

以下内容默认不得进入普通 Brain：

```
.env
API Key
Token
Cookie
Password
Private Key
*.pem
*.key
Credential
Session
```

发现疑似 Secret：

```
secret_skipped
```

但日志中不得记录 Secret 值。

未来密码类数据应该进入独立 Secret Manager。

***

# 26. Project Brain

Personal Brain 内必须包含 Project Brain 子系统。

目标：

解决：

```
换 TRAE 账号
换 Cursor
换 AI
上下文压缩
```

后项目失忆。

***

# 27. Project 基础对象

至少支持：

```
Project
Module
Task
Checkpoint
Decision
Constraint
ChangeEvent
Milestone
```

***

# 28. Project Profile

保存：

```
项目是什么
目标
核心理念
技术栈
架构
全局约束
部署方式
目录概览
```

***

# 29. Module Card

每个模块维护当前状态。

例如：

```
MODULE: memory

path:
src/memory/

responsibility:
长期记忆管理

core_files:
...

interfaces:
...

dependencies:
...

used_by:
...

constraints:
...

indexed_commit:
...

file_hashes:
...

freshness:
fresh / stale
```

***

# 30. Module Card 不代替源码

AI 修改模块前：

必须读取当前目标源码。

Module Card 用于：

```
快速理解
导航
恢复上下文
了解历史
```

***

# 31. V1 不做源码全文向量化

明确禁止第一版扩大范围。

V1 只保存：

```
Module Card
接口摘要
模块关系
Task
Decision
Constraint
Change
```

源码全文 RAG 属于后续功能。

***

# 32. Git 是历史权威

历史源码交给：

```
Git
```

Personal Brain 保存：

```
为什么改
什么时候改
改了什么
影响什么
对应 Commit
```

不要复制每个历史源码版本到数据库。

***

# 33. Git 工作区

不能只依赖 Commit。

同时检查：

```
HEAD
git status
git diff
dirty files
```

因为用户可能工作很久没有 Commit。

***

# 34. Module Freshness

如果：

```
Module Card indexed_commit = A

Current HEAD = B
```

且相关文件发生变化：

```
freshness = stale
```

查询模块时必须返回：

```
STALE WARNING
```

禁止把过期知识冒充事实。

***

# 35. 增量同步

第一次：

```
Full Bootstrap
```

之后：

只处理变化文件。

例如：

```
memory/writer.py changed
memory/manager.py changed
```

只更新：

```
memory module
```

禁止每次重扫整个仓库。

***

# 36. Project Bootstrap

实现：

```
bootstrap_project()
```

首次扫描输出：

```
Project Profile
Module Map
Module Cards
Dependencies
API Overview
Database Overview
Config Overview
Known Constraints
Known TODO
Known Risks
```

尊重：

```
.gitignore
```

过滤：

```
node_modules
venv
.venv
dist
build
cache
巨大日志
二进制
模型文件
IDE cache
```

***

# 37. 旧项目文档迁移

用户现有：

```
PLAN.md
HISTORY.md
PROJECT_CONTEXT.md
需求文档
背景文档
```

第一次 Bootstrap：

读取但不删除。

尝试提取：

```
Project Profile
Decision
Constraint
Task History
```

无法确认的信息：

```
confidence = low
```

不能把 AI 猜测包装成事实。

***

# 38. Task 生命周期

Task 状态：

```
planned
active
paused
completed
failed
cancelled
```

重大任务使用：

```
start_task()
checkpoint_task()
finalize_task()
```

***

# 39. Start Task

记录：

```
goal
start_git_head
dirty_state
affected_modules
plan
constraints
```

***

# 40. Checkpoint

长任务不能等两小时以后才保存。

阶段完成时保存：

```
current_head
dirty files
changed files
completed
current problems
important decisions
next step
test result
```

***

# 41. Finalize Task

任务完成后自动比较：

```
start state
vs
end state
```

生成 Task Report：

```
目标
开始 Commit
结束 Commit
修改文件
模块变化
接口变化
为什么改
测试结果
未完成事项
```

并：

```
更新 Module Card
生成 Change Event
保存重要 Decision
保存 Constraint
更新索引
```

避免重复写同一 Decision。

***

# 42. 上下文压缩恢复

系统不得依赖：

```
TRAE 通知“我要压缩上下文”
```

必须独立运行。

如果聊天被压缩或账号更换：

新的 Agent 可以：

```
get_project_context
get_active_task
get_module_context
get_recent_changes
```

重新恢复。

***

# 43. 新账号核心验收场景

旧 TRAE：

完成两小时任务。

关闭。

换全新 TRAE 账号。

完全没有聊天历史。

用户只说：

```
继续昨天的任务。
```

Agent 必须通过 Personal Brain 获取：

```
项目背景
Active Task
已经完成
剩余内容
相关 Module
Decision
Constraint
最近变化
Git 状态
```

并继续工作。

这是 V1 的最高验收之一。

***

# 44. Brain Bridge

Windows / 本地开发机器运行：

```
Project Brain Bridge
```

建议 Python 实现。

职责：

```
识别当前 Workspace
读取 Git
读取 Files
读取 Diff
读取 Symbol / 基础结构
连接远程 Personal Brain
```

***

# 45. Bridge 安全

Bridge 必须限制：

```
workspaceRoot
```

禁止：

```
../
```

目录逃逸。

AI 不能通过 Bridge 随意读取：

```
C:\
浏览器目录
其他项目
整个用户目录
```

***

# 46. Git Remote

对于真正跨设备继续项目：

推荐有中央 Git Remote。

可以使用：

```
现有 GitHub Private
GitLab
其他 Git Remote
```

如果没有：

可部署：

```
Gitea
```

但 Personal Brain 不依赖 Gitea 才能运行。

***

# 47. 未 Commit 代码边界

Personal Brain 不承诺自动保存所有未 Commit 源码。

Bridge 可以记录：

```
HEAD
dirty
changed files
diff summary
```

真正跨设备恢复代码：

推荐：

```
WIP checkpoint commit
+
push
```

***

# 48. Personal Brain Server 技术栈

V1 推荐：

```
Linux
Docker Compose

Python Brain Server

PostgreSQL

pgvector

Local Filesystem Asset Storage

MCP

Local Brain Bridge
```

***

# 49. 不要在 V1 引入

除非明确证明必要：

```
Kafka
RabbitMQ
Redis
ElasticSearch
Neo4j
Qdrant
MinIO
复杂微服务
```

***

# 50. PostgreSQL

PostgreSQL 是：

```
结构化长期权威数据库
```

建议表至少考虑：

```
projects
modules
module_versions

tasks
checkpoints
decisions
constraints
change_events

raw_inputs
documents
assets

events
entities
relations

expenses
todos

memories
preferences
preference_evidence
profile_history

clients
permissions
audit_logs

inbox

sync_state
jobs

embedding_metadata
```

不要求完全按这些名称实现。

但概念不得缺失。

***

# 51. pgvector

V1 使用：

```
PostgreSQL + pgvector
```

用于语义检索。

不要额外部署 Qdrant。

***

# 52. PostgreSQL Full Text Search

文本搜索使用：

```
PostgreSQL FTS
+
pgvector
+
Metadata Filter
```

V1 不需要 Elasticsearch。

***

# 53. Relation Graph

V1：

使用 PostgreSQL：

```
relations
```

表表达图关系。

暂时不引入 Neo4j。

***

# 54. Background Worker

V1：

```
Brain API
+
Brain Worker
```

使用 PostgreSQL jobs 表作为轻量队列。

任务例如：

```
Document parsing
Embedding
Module refresh
Maintenance
Digest
```

不要为了队列立即部署 Redis/Kafka。

***

# 55. Brain Server 分层

建议：

```
MCP
 ↓
Brain Service
 ↓
Domain Service
 ↓
Repository
 ↓
PostgreSQL
```

禁止：

```
MCP Tool 内直接大量写 SQL。
```

MCP 是协议层。

不是业务核心。

***

# 56. MCP

需要支持：

```
Remote MCP

Local stdio Bridge
```

执行时必须查当前官方 MCP 规范和官方 SDK。

不要复制旧博客中的过时实现。

***

# 57. V1 MCP 工具

不要一次做几十个工具。

核心建议：

```
get_brain_context

search_brain

save_note

get_self_context

add_expense

get_expense_summary

add_todo

list_todos

complete_todo

get_project_context

get_module_context

search_project

get_active_task

start_task

checkpoint_task

finalize_task

record_decision

record_constraint

get_recent_changes

sync_workspace

check_freshness

upload_asset
```

如果职责重复：

合并。

***

# 58. Query Router

用户问题不能全部扔向量库。

先判断 Intent。

例如：

```
“这个月花多少钱”
→ finance SQL

“今天有什么事”
→ todo SQL

“以前为什么做这个设计”
→ project history

“我之前有没有写过某个想法”
→ semantic / FTS
```

***

# 59. Retrieval

最终检索排序至少考虑：

```
semantic relevance

keyword match

recency

confidence

source trust

importance

current validity

scope match
```

***

# 60. 当前事实 vs 历史事实

如果用户问：

```
现在 memory 模块怎么工作？
```

优先：

```
current Module Card
```

历史只补充。

如果问：

```
为什么变成现在这样？
```

才拉：

```
旧状态
Decision
Task
Current
```

***

# 61. Hierarchical Retrieval

必须支持渐进检索。

例如项目：

```
Project Summary
 ↓
Module
 ↓
Task / Decision
 ↓
Raw / Git
```

个人数据：

```
Self Summary
 ↓
Preference
 ↓
Evidence
 ↓
Original Document
```

不要一次塞全部原始内容。

***

# 62. Context Compiler

实现：

```
Context Compiler
```

职责：

```
权限检查
Scope
去重
排序
Freshness
冲突检查
Token Budget
压缩
Source IDs
```

最终生成：

```
Context Package
```

***

# 63. Token Budget

Context Compiler 必须限制上下文大小。

例如：

```
summary
normal
deep
```

不同 Detail Level。

普通恢复不要返回几十万 Token。

***

# 64. Context Package

项目例子：

```
Intent:
resume_task

Project:
...

Current State:
...

Active Task:
...

Relevant Modules:
...

Important Decisions:
...

Constraints:
...

Recent Changes:
...

Warnings:
...

Suggested Next:
...
```

***

# 65. Scope

查询必须支持：

```
personal
projects
finance
diary
music
project:<id>
module:<id>
```

***

# 66. Client 权限优先于搜索

流程：

```
Request
 ↓
Authentication
 ↓
Client Permission
 ↓
Scope
 ↓
Search
```

禁止先搜到敏感内容再过滤。

***

# 67. 不同客户端不同权限

统一 Brain ≠ 统一权限。

例如：

```
TRAE

projects/*
read/write

diary/*
no

finance/*
no
```

Personal Agent：

```
personal/*
read/write

finance/*
read/write

projects/*
read
```

临时客户端：

```
read-only
limited scope
```

***

# 68. Client Authentication

每个客户端独立：

```
client_id
token
allowed_scopes
allowed_tools
last_used_at
revoked_at
```

Token 数据库存 Hash。

日志不能输出完整 Token。

***

# 69. Privacy Sensitivity

每个对象可带：

```
normal
personal
private
highly_private
secret
```

Secret 不进入普通 Brain。

***

# 70. Audit

记录：

```
哪个 Client
何时
调用哪个 Tool
访问哪个 Scope
成功/失败
耗时
```

默认不要记录：

```
完整私密正文
完整源码
Token
Secret
```

***

# 71. Trilium

Trilium 是：

> 人类人工编辑与浏览界面。

它不是 Personal Brain 本体。

用途：

```
写日记
写笔记
个人哲学
工作文档
浏览部分知识
```

V1 不做复杂双向同步。

优先：

```
可靠单向导入
或
有限同步
```

避免冲突系统过早复杂化。

***

# 72. Khoj

V1 不依赖 Khoj。

未来可以作为：

```
AI Knowledge UI
```

但删除 Khoj 后：

```
Brain
MCP
TRAE
Cursor
Memory
```

仍然必须正常。

***

# 73. Inbox

无法确定的信息进入：

```
Brain Inbox
```

例如：

```
不确定的 Todo
低置信度分类
Entity 合并
核心哲学候选变化
冲突内容
```

系统能自己处理的不要打扰用户。

***

# 74. Proactivity

V1 主动能力必须克制。

第一版只主动处理：

```
Todo deadline

Project sync/index failure

Backup/storage/Brain health failure
```

其他：

```
消费异常
审美变化
重复 Bug
兴趣趋势
```

V1 可以检测或存储，但不要主动频繁通知。

***

# 75. 主动性原则

必须：

```
Detect != Notify
```

发现事情不代表一定打扰用户。

主动通知必须有明确 Trigger。

***

# 76. Risk Level

未来 Tool 可带：

```
risk_level
```

例如：

```
read = low

add note = low

add expense = low

profile modification = medium

external message = high

delete data = high
```

高风险操作必须确认。

***

# 77. Notification Center

主动消息不能各模块自己乱发。

统一：

```
Notification Center
```

负责：

```
优先级
冷却
合并
重复过滤
渠道
```

***

# 78. Daily / Weekly / Monthly Digest

支持层级摘要概念：

```
Raw
 ↓
Daily
 ↓
Weekly
 ↓
Monthly
```

摘要只作为导航。

不能替代 Raw 数据。

V1 可以先实现 Daily Digest 或预留结构。

不要因为 Digest 未完成阻塞核心上线。

***

# 79. Source Trust

为数据来源建立权重概念。

例如：

```
explicit user statement
> original document
> system fact
> AI extraction
> AI inference
```

数值如何实现可以自行设计。

***

# 80. Brain Maintenance

Personal Brain 必须考虑长期运行。

不能只会写入。

要支持：

```
去重
归档
候选升级
冲突检测
Stale 修复
索引重建
备份
完整性检查
```

***

# 81. Canonical vs Derived

所有数据明确：

```
canonical
derived
```

例如：

```
日记原文 = canonical

summary = derived

embedding = derived

self inference = derived
```

Derived 全部可以重建。

***

# 82. Memory Promotion

候选偏好随着 Evidence：

```
candidate
↓
established
```

必须基于：

```
Evidence Count
Time Span
Source Trust
Consistency
```

不能单纯因为重复两次就永久定型。

***

# 83. Memory Demotion

长期无证据或出现相反证据：

```
active
↓
historical
```

不是直接 DELETE。

***

# 84. 去重

不能简单：

```
语义相似 → 删除
```

应该：

```
多 Evidence
 ↓
Canonical Conclusion
```

Evidence 保留。

***

# 85. Data Lineage

派生数据必须知道：

```
derived_from
```

例如：

```
Document
 ↓
Summary
 ↓
Preference Evidence
 ↓
Self Model
```

这样删除源数据时可以正确处理派生数据。

***

# 86. 删除

用户明确要求彻底删除某内容时：

必须追踪：

```
原文
Embedding
Summary
Relations
Evidence
Caches
```

并正确处理。

不能只删一张主表。

***

# 87. 删除 Evidence 后重新计算

例如：

```
Preference
有 4 个 Evidence
```

删除一个：

重新计算 confidence。

不一定删除 Preference。

***

# 88. Brain Doctor

提供：

```
brain doctor
```

或等效 Health Check。

检查：

```
Database
Assets
Hash
Broken Relations
Missing Embeddings
Stale Modules
Candidate Backlog
Conflicts
Jobs
Backup
Disk Space
```

***

# 89. Asset Integrity

每个 Asset 保存：

```
SHA256
size
```

定期检查：

```
missing
corrupted
hash mismatch
```

***

# 90. Backup

这是强制要求。

至少备份：

```
PostgreSQL
Assets
Config
必要的 Git
Trilium
```

数据库使用：

```
pg_dump
```

不要仅复制运行中的 PostgreSQL 数据目录作为唯一备份。

***

# 91. Restore Verification

不能只有：

```
Backup Successful
```

至少提供恢复文档。

最好定期在隔离环境：

```
Restore DB
Verify
Check Asset Hash
```

形成：

```
Last Restore Verified
```

***

# 92. Backup Retention

合理设计，例如：

```
Daily
Weekly
Monthly
```

具体数量根据磁盘空间调整。

不能硬编码不合理值。

***

# 93. Schema Migration

数据库变化必须使用迁移体系。

流程：

```
Backup
 ↓
Migration
 ↓
Validation
 ↓
Health Check
```

禁止生产数据库人工随便 ALTER 后不记录版本。

***

# 94. Derived Model Versioning

派生内容保存生成版本。

例如：

```
embedding_model
parser_version
profile_model_version
summary_version
```

以后可以逐渐重建旧数据。

***

# 95. Reprocessing

未来允许用更好的模型重新解析旧内容。

因为：

```
Raw 原始数据一直在。
```

但重分析不能篡改历史 AI 推断。

旧解释可以：

```
superseded
```

新解释：

```
active
```

***

# 96. Maintenance 成本

能够普通代码完成的：

```
Hash
SQL stats
Stale
Backup Check
Dedup Candidate
```

不要调用 LLM。

原则：

```
Deterministic First
AI When Needed
```

***

# 97. Docker

家庭 Linux Server 使用：

```
Docker Compose
```

推荐目录：

```
/opt/personal-brain/
```

例如：

```
/opt/personal-brain/
├── compose.yml
├── .env
├── brain/
├── worker/
├── postgres/
├── assets/
├── trilium/
├── backups/
├── scripts/
└── docs/
```

根据真实环境调整。

***

# 98. Docker 核心服务

V1 尽量控制为：

```
personal-brain-api

personal-brain-worker

postgres-pgvector

trilium
```

如果需要：

```
gitea
```

Reverse Proxy：

如果已有：

复用。

不要重复部署。

***

# 99. 网络

首先检测：

```
Nginx
Caddy
Nginx Proxy Manager
Traefik
Cloudflare Tunnel
Tailscale
```

优先复用。

数据库禁止公网暴露：

```
0.0.0.0:5432
```

***

# 100. Remote MCP

外部：

```
ChatGPT
手机
远程 AI
```

通过：

```
HTTPS
Authentication
MCP
```

访问。

具体协议以执行时当前 MCP 官方规范为准。

***

# 101. Local Bridge

本地开发：

```
TRAE
Cursor
```

优先通过：

```
stdio MCP Bridge
```

访问本地 Workspace + 远程 Brain。

执行时查询 TRAE/Cursor 当前官方 MCP 配置规范。

不要凭记忆使用过期字段。

***

# 102. 网络边界

家庭服务器即使公开 MCP：

也只公开：

```
Brain API/MCP
```

不能公开：

```
PostgreSQL
内部 Worker
内部管理接口
```

***

# 103. V1 成功场景一：跨账号项目恢复

旧 TRAE：

完成重大任务。

换全新账号。

输入：

```
继续之前的项目。
```

新 Agent 能恢复：

```
Project
Task
Modules
Decision
Constraint
Git State
Remaining Work
```

PASS。

***

# 104. V1 成功场景二：跨客户端生活记录

手机：

```
午饭38，打车26，记一下。
明天下午取快递。
```

晚上其他客户端问：

```
今天花了多少？
明天有什么事？
```

同一 Brain 正确返回。

PASS。

***

# 105. V1 成功场景三：文档 + 偏好

上传旅行感想 + 图片。

系统：

```
保存 Raw
建立 Event
保存 Asset
建立 Candidate Preference
```

以后问：

```
我以前是不是提过更喜欢人少一点的旅行环境？
```

能找到 Evidence。

但不会因为一次旅行把它变成绝对人格标签。

PASS。

***

# 106. V1 成功场景四：A/B/C

明确：

```
记住，以后项目时间统一北京时间。
```

直接正式保存。

普通：

```
最近挺喜欢爵士乐。
```

Candidate。

重要哲学改变：

必须确认。

PASS。

***

# 107. V1 成功场景五：Module Stale

Module Card：

```
indexed HEAD = A
```

修改对应模块。

立即：

```
stale
```

同步后：

```
fresh
```

AI 查询时不会拿旧摘要当事实。

PASS。

***

# 108. V1 成功场景六：上下文压缩

Task 进行中。

已经产生多个 Checkpoint。

模拟：

```
完整聊天历史丢失。
```

新 Agent 仅通过 Brain：

恢复任务。

PASS。

***

# 109. V1 成功场景七：权限

TRAE Client 尝试读取：

```
private diary
```

如果 Scope 不允许：

必须拒绝。

并 Audit。

PASS。

***

# 110. V1 成功场景八：Secret

项目包含测试：

```
.env
API_KEY=...
```

Bootstrap 后：

Brain 搜索不得返回 Secret。

PASS。

***

# 111. V1 成功场景九：断网

Brain Server Offline。

Bridge 必须明确：

```
Personal Brain Offline
```

不能假装 Memory 保存成功。

允许：

```
pending sync
```

恢复后重试。

PASS。

***

# 112. V1 明确不做

第一版禁止范围膨胀到：

```
源码全文向量化

复杂 Neo4j 图谱

完整视频理解

自动付款

自动投资

复杂健康分析

高风险自主行为

全盘扫描电脑

复杂人格自动诊断

全天候大量主动提醒

所有媒体一次性深度 AI 分析

多 Agent 自治社会

复杂双向 Trilium 同步
```

这些属于 V2+。

***

# 113. Long-Term Reserved

架构必须为未来预留：

```
Code RAG

Advanced Knowledge Graph

Music Analysis

Image Aesthetic Evolution

Annual Personal Review

Advanced Proactivity

Multiple Storage Backend

Advanced Agents

Calendar Integration

Email Integration

Finance Integrations
```

但 V1 不实现。

***

# 114. 开发纪律

非常重要：

不要：

> 一次性写几十个模块，然后最后一起测试。

必须：

```
理念冻结
 ↓
核心闭环
 ↓
行为验证
 ↓
再扩展
```

***

# 115. Phase 0：环境调查

首先：

只调查。

不要修改服务器。

检查：

```
uname -a
cat /etc/os-release
uname -m

docker --version
docker compose version
docker ps
docker network ls

df -h
free -h
ss -lntup
```

检查：

```
反向代理
数据库
Git
Trilium
Tailscale
Cloudflare
Docker
磁盘
CPU
RAM
端口
```

然后输出：

```
Environment Report
```

***

# 116. Phase 1：最小 Brain Core

实现：

```
PostgreSQL
pgvector
Brain API
Auth
Client
Migration
Basic Audit
```

核心对象先完成：

```
Raw Input
Document
Memory
Event
Expense
Todo
```

验收后再继续。

***

# 117. Phase 2：MCP

先实现最少工具：

```
search_brain
save_note
add_expense
get_expense_summary
add_todo
list_todos
```

验证多个测试客户端。

***

# 118. Phase 3：Asset / Document Intake

实现：

```
Upload
Hash
Metadata
Document Extraction
FTS
Embedding
Secret Filter
```

先支持常见文本/PDF/图片。

***

# 119. Phase 4：Self Model

实现：

```
Candidate Preference
Evidence
A/B/C
Confidence
Supersede
History
```

这是重点验收阶段。

***

# 120. Phase 5：Event / Relation

实现：

```
Timeline
Entities
Relations
```

不做复杂图数据库。

***

# 121. Phase 6：Project Brain

实现：

```
Project
Module
Task
Checkpoint
Decision
Constraint
Git State
```

***

# 122. Phase 7：Bridge

Windows 优先。

实现：

```
Workspace
Git HEAD
Status
Diff
File Change
Brain Connection
```

***

# 123. Phase 8：增量 Project Sync

实现：

```
Bootstrap
Stale Detection
Changed Files
Module Update
```

***

# 124. Phase 9：Context Compiler

实现：

```
Query Router
Hybrid Retrieval
Context Package
Token Budget
Permission
Freshness
```

***

# 125. Phase 10：TRAE 接入

使用执行时最新 TRAE 官方 MCP 配置。

验证：

```
new account
new chat
resume task
```

***

# 126. Phase 11：Cursor / 其他客户端

验证：

```
TRAE 写入 Decision

Cursor 查询 Decision
```

反向也一样。

***

# 127. Phase 12：Trilium

部署/接入人工 Knowledge UI。

优先可靠。

不做复杂双向同步。

***

# 128. Phase 13：Maintenance

实现：

```
Brain Doctor
Backup
Restore Docs
Asset Integrity
Job Health
Inbox
Stale Repair
```

***

# 129. 每阶段必须有 Given / When / Then

例如：

```
Given:
Brain 空数据库

When:
添加 Expense 38

Then:
查询当月 Expense
必须增加 38
```

禁止用：

```
“看起来应该可以”
```

作为验收。

***

# 130. 测试

至少包括：

```
Unit Test

Integration Test

Database Migration Test

Permission Test

MCP Test

Backup Restore Test

Cross Client Test
```

***

# 131. Bug 处理

遇到错误：

```
读取日志
 ↓
确定根因
 ↓
最小修改
 ↓
测试
 ↓
记录根因
```

禁止：

```
chmod 777

关闭认证

privileged true

删库重装

随便删 Volume
```

作为第一解决方法。

***

# 132. 安全危险操作

以下必须谨慎：

```
删除大量数据
删除 Docker Volume
数据库 DROP
覆盖 Git 历史
Force Push
批量覆盖文件
修改防火墙
修改系统级认证
```

必须保证可恢复。

***

# 133. Git

Personal Brain 项目本身也必须使用 Git。

不得 Commit：

```
.env
Secret
Database Data
Private User Data
Tokens
```

***

# 134. Logging

Docker / Application 日志要限制增长。

禁止几年以后 JSON Log 占满磁盘。

配置：

```
rotation
max size
max files
```

***

# 135. Health

提供健康状态，例如：

```
Brain API:
healthy

Database:
healthy

Worker:
healthy

Indexer:
healthy

Last Backup:
...

Restore Verified:
...

Stale Modules:
...

Failed Jobs:
...

Disk:
...
```

***

# 136. Dokumentation

最终至少生成：

```
README.md

ARCHITECTURE.md

DATA_MODEL.md

MCP_TOOLS.md

SECURITY.md

DEPLOYMENT.md

BACKUP_RESTORE.md

TRAE_SETUP.md

CURSOR_SETUP.md

PERSONAL_BRAIN_RULES.md

PROJECT_BRAIN_RULES.md

OPERATIONS.md
```

***

# 137. PERSONAL\_BRAIN\_RULES

生成给 AI 客户端使用的规则。

至少要求：

```
不要默认把每句话存长期记忆

明确“记住” → 正式保存

普通偏好 → Candidate

重要哲学变化 → 确认

项目复杂任务 → Task

阶段 → Checkpoint

完成 → Finalize

使用模块知识前检查 Freshness

真正改代码前读当前代码

AI推断不能冒充事实

结构化问题优先结构化查询
```

***

# 138. 记忆提取原则

长期保存优先：

```
明确要求

重大 Decision

Constraint

Bug Root Cause

Important Requirement

Long-term Preference

Important Event

Project Milestone

Useful Personal Knowledge
```

不要长期保存：

```
普通寒暄

临时 Debug 输出

重复内容

无价值 AI 推理

AI未经证实猜测
```

***

# 139. Personal Brain 的最终行为哲学

必须遵循：

> 事实忠实保存。

> 推断保留证据。

> 普通偏好渐进确认。

> 重大价值观谨慎更新。

> 用户明确陈述具有最高权重。

> 过去不会因为现在改变而被抹掉。

> 当前信息与历史信息严格区分。

> 摘要永远不能取代原始数据。

> 搜索应该返回最需要的内容，而不是最多的内容。

> AI应该理解什么时候不需要读取某些信息。

> 主动性必须有目的，而不是刷存在感。

> 不同客户端共享一个 Brain，但不共享无限权限。

> Personal Brain 属于用户，不属于任何 AI 厂商。

***

# 140. 最终完成定义

Personal Brain V1 不是：

```
网页能打开

Docker Container Up
```

就算完成。

真正完成意味着：

用户可以：

```
家里：
Windows + TRAE

外面：
ChatGPT / Mobile

另一天：
Cursor
```

持续使用同一个 Brain。

用户换 TRAE 账号后：

项目仍然有连续性。

用户换 AI 后：

个人长期资料仍然存在。

用户问过去发生过什么：

Brain 能沿 Timeline 回答。

用户问这个月花了多少钱：

Brain 精确统计。

用户说记住某项长期规则：

所有后续客户端都能使用。

用户上传文档：

未来任意授权客户端都能搜索。

用户偏好变化：

Brain 能保留过去与现在，而不是覆盖历史。

项目模块变化：

Brain 知道旧知识已经 stale。

聊天上下文被压缩：

Brain 可以恢复当前 Task。

***

# 141. 开始执行方式

现在开始：

## Step 1

只执行：

```
Phase 0 Environment Investigation
```

不要直接安装一堆组件。

输出：

```
Environment Report
```

并基于真实服务器环境生成：

```
Implementation Plan V1
```

***

## Step 2

确认不存在环境冲突后：

从：

```
Brain Core
```

开始。

***

# 142. 与用户交互原则

只有真正无法自行获取的信息才向用户索取，例如：

```
SSH 权限
服务器地址
域名
现有项目路径
必要 API Key
```

能够：

```
检查
读取
测试
验证
查官方文档
```

自行解决的问题不要不断抛给用户。

***

# 143. 信息来源

任何：

```
MCP
TRAE
Cursor
Trilium
Docker Image
SDK
```

版本和配置方式：

在实际执行时检查当前官方文档。

不要依赖几年前的博客。

不要因为本提示词提到一个具体版本，就假定执行时仍然是最新版本。

***

# 144. 最终原则

这个系统应该能够运行很多年。

因此优先：

```
简单
可验证
可迁移
可备份
可恢复
可追溯
平台无关
数据属于用户
```

而不是：

```
组件最多
架构最炫
技术最复杂
```

最终目标：

```
账号会更换。

AI 会更换。

设备会更换。

软件会更换。

模型会更换。

聊天上下文会消失。

但：

用户自己的长期记忆不消失。

个人知识不消失。

项目历史不消失。

思想演化不消失。

文件不消失。

工作进度不消失。

真正长期存在的是：

Personal Brain。
```

现在开始 Phase 0。
