# 《档案馆的诚实账本》

## 开场：这次不吹

你问得很准："**我也说了这么多漂亮的理论，那程序真的做到了吗？**"

所以我这次做的不是再讲一遍故事，而是**翻账本**：把我在故事里许过的每一条承诺，拿到库里、代码里、真实运行里逐条对账。账本分三档标记，不许含糊：

- **✅ 真活里跑过**——有真实数据为证，不是测试；
- **⚠️ 有工具但没赶上真事**——代码在、测试过，但真实生活里还没发生过那种事（缺样本或缺时间）；
- **⛔ 还只在图纸上**——没接线、或压根没做。

另外我要先交代一件事：**本机那个实例（18082）在 18:41 被正常停机了**（不是崩溃，是标准的"关机"信号，编号文件也被清干净了，看起来像你自己或另一个会话主动停的）。我没动它。所以本机证据我引用的是**停机前**已经跑出来的（18:03 的实测）；其余对账用的是**你真正的生产实例**，它一直在跑。

---

## 一、真实库里的数字（今天下午 19 点前后）

先看"库里真的有什么"，再看故事：

- **检索卡片 35 张，35 张全带语义指纹**（指纹版本：豆包向量 1024 维）——"两个柜子都真有货"，✅；
- 卡片分两类：原生记录 28 张 + 关于你本人的画像 7 张；
- **关于你的便签：6 条候选、1 条 A 类**。那条 A 类（"倾向把项目、数据和记忆统一收拢到自有服务器体系"）是**今天 18:16 被夜班巡逻自动激活的**，便签背面写着激活时间和理由（`class_a_explicit`）——✅ 留痕是真的；
- **通知 1 条：已送达**（"有 2 个作业失败"）——✅ 主动提醒是真的；
- **今天 19:13，你的客户端自己写了 7 条笔记**：7 条索引成功、7 条抽取成功、7 条派生内容进了库（真实模型抽的）。这是**真实使用的第一批量**，✅ 核心链路通了。

---

## 二、逐条对账（故事承诺 → 现实）

| 我在故事里说的 | 账本真相 |
|---|---|
| "你只交内容，编目自动完成" | ✅ 真活里跑过（19:13 那 7 条就是证据） |
| "写入即回执、原文永存" | ✅ 真活里跑过（失败的那条原文也好好在库） |
| "编目是死逻辑：拆词 + 指纹" | ✅ 真活里跑过（35/35 带指纹） |
| "两个柜子融合排序、给出排序理由" | ✅ 早前实测过 + 28 项排序测试通过 |
| "卡片带身份牌，查之前先验牌" | ✅ 代码核验 + 11 项授权测试 |
| "卡片只是副本，随时能重建" | ⚠️ **半档**：副本性 ✅；但"重建那个按钮"**从来没人真按过**（没有入口） |
| "解读员只猜不当真，攒够证据才升级" | ⚠️ 机制 ✅；但**真实生活里至今 0 次升级**（6 条候选全在候选墙上等时间） |
| "夜班巡逻每天自动干活" | ⚠️ **只过了一个晚上**：部署后第一次巡夜（18:16）六件事全干成了；跨天还没经历 |
| "每天一页摘要，能查、能溯源" | ✅ 本机真实生成过（来源 3/3 命中）；生产当天没有前一天的记录，它**诚实跳过**而不是编 |
| "有故障主动递纸条，没事绝不吵你" | ✅ 真活里跑过（送达 1 条；健康那天 0 条） |
| "偏好变了：旧的不撕、反着说就进待办、换口味靠灰尘" | ⚠️ **两处没实战**：冲突通道**真实数据里 0 次触发**（只在测试里制造过）；"灰尘"（90 天/180 天）**从没等到真实时间过去** |
| "项目变更有版本照片、旧照片盖过期章" | ⚠️ 代码里有这个机制（也测过）；但你还没把真实仓库接进来同步过 |
| "分房间存、账目走精确账本" | ✅ 早前实测过 30 个工具；这轮没复测 |
| "三道防串门" | ✅ 前两道核验过；第三道（日报分房间）只在本机单房间实测 |
| （我承认过的两个缺口）"替代型变更不识别""没有连线图" | ✅ 现在依然如此，没消失 |

---

## 三、今天翻出来的"新账"：一条半条命的笔记

对账时撞见一件真事，必须讲：

今天 19:13，你写了一条 1308 字的笔记（标题是"【结论笔记·工具建设】测试版 Zafiro 打通 Personal Brain…"）。它现在**躺在库里，却搜不到、也抽不出**——两个作业双双进了"死信箱"，原因写着"疑似机密"。

用档案馆的话说：

> 纸条递进来了，前台收下了；可编目员和解读员拿起它一看——**里面写着像"password: 一串东西"的字样**，
> 于是两个人都把手缩回去了：**"这像是别人家的钥匙，我们不能把它抄给外面的机器看。"**

**这一步做得对**：内容没有被发给我们之外的任何模型（这是当初立下的保密规矩）。
**但这里有个真毛病**：门口的保安本来该在**收纸条的时候就提醒你**，可他没有——**内容先进了库，才在"要外发"的路口被拦下**。结果就是"半条命"：原件在、检索没卡、抽取没做。

顺带两条旧账：还有 2 条"资产解析失败"死信（17:30 的，跟这次改动无关），加上刚才这 2 条，**死信箱现在 4 条**——明天早上 8 点的巡夜会再给你递一次提醒。

---

## 四、所以，"漂亮理论"落到几成？

一句话：**核心链路（收→编目→检索→抽取→巡夜→提醒）已经在你的真实数据上跑通了；但有 4 处还没被真实生活检验过**：

1. **时间没到**：升级要跨 14 天、灰尘要 90/180 天——机制在，等时间；
2. **样本没到**：冲突/改主意这类事，你还没在真实记录里写过（测试里制造过）；
3. **按钮没人按过**：重建、真实仓库同步（版本照片）；
4. **接线有错位**：保密门禁只在"外发口"守，没在"写入口"守 → 半条命笔记。

没有一条是"设计跑偏"，全是"时间/样本/接线"问题——这是好消息，也是坏消息（好消息=架构没歪；坏消息=不能说"全都好了"）。

---

## 五、要你拍两件事（我不擅自动手）

**决定一：含疑似凭据的笔记怎么处理？**（就是那条半条命）
- (a) **门口就拒收**：像上传资产那样，写入时直接告诉你"这条含疑似凭据，已拒收"；
- (b) **收下但隔离**：原文留在库里、**只做本地关键词卡片**（不外发、不抽指纹、不送模型），卡片上盖一个"疑似凭据，语义通道已跳过"的标签，**你还能搜到它**。[我推荐 (b)：原文已经在你库里了，本地关键词并不新增暴露面，反而救回了"搜不到"]

**决定二：顺手修两个小病吗？**
- 把死信原因从"job execution rejected or failed"改成看得懂的话（比如"疑似凭据，已拦截外发"）；
- 给"重建"一个能真按的按钮（入口），并按一次给你看。

你回一句"一选 b、二都修"这种就行。清账本的完整对账表在 [01-思考路径.md](file:///e:/新建文件夹/Personal-Brain-V1/docs/tasks/design-20260925-功能落地核查/01-思考路径.md)；这一轮部署的机检证据在 [backend-activation-2026-09-25.md](file:///e:/新建文件夹/Personal-Brain-V1/docs/acceptance/backend-activation-2026-09-25.md)。

**总账一句话：故事没有骗你，但故事把"刚接好线、还没通电多久"说成了"一直在亮"——今天起，账本按证据等级记，✅/⚠️/⛔ 一个都不含糊。**

---

## 六、追加：手机端（Zafiro）实测八条缺陷的回执（2026-09-25 夜）

你手机端的八个毛病，我按"谁的代码决定这条链能不能通"分了类：

- **三条是服务端的账，已修并当场验证**：
  1. **治理工具一律 SCOPE_DENIED**：真相是"删除/批阅"这类工具按**数据域**授权（比如 knowledge），
     而新客户端的默认档只在 `review` 这个域上发了通行证——所以门还没摸到就被拦。
     现在多了一个**运维开关**（`review-access`，双确认、留审计、可收回）：我给你的 zafiro 客户端
     开了 knowledge/self 两把钥匙。实测：**建删除计划不再被拒**，没开钥匙的 finance 仍然被拒（没有放松边界）。
  2. **画像里一堆重复 claim**：抽取管道以前只"往里放"不"回头看"。现在三管齐下——
     抽取时先比对（重复的就并到已有那条、证据算它一份）、每晚 04:50 有**清道夫**扫全库
     （完全相同/几乎同字的自动合并，留合并痕迹、不删行）、说不清是不是同一条的进 Inbox 让你点。
     首次在真实库上跑：34 条扫出 1 组、合并 2 条（就是你报的那句"收拢到自有服务器"的重复），
     另外 1 条近义进了 Inbox。
  3. **分叉对话重复归档**：分叉是 app 的事，服务端看不见；但服务端现在能接住你的"这是同一条"声明——
     归档时带上 `content_hash`，服务端**复算一遍**确认真是同一条才折进已有记录（内容不同一律拒绝折叠）。
- **五条是 app 侧的账**（D4 标题越叠越长、D5 密钥明文、D6 缺客户端脚本、D7 只能直读 SQLite、
  D8 脚本告警）：没有源码我不能替你改 app，逐条建议写在
  [01-思考路径.md](file:///e:/新建文件夹/Personal-Brain-V1/docs/tasks/design-20260925-功能落地核查/01-思考路径.md) 的第 9.3 节。**其中 D5 是安全问题，建议优先**。

**顺手翻出来的三笔暗账**（不修的话上面的修复也会半途而废）：
- 客户端能"发起"确认却**看不到也点不动**（`list_review_items` / `resolve_review_item` 没暴露）→ 你已拍板，工具面 30→32；
- 高风险门禁"要确认才能确认"的**死锁**（建计划和批准都直接被判"需要确认"）→ 门禁搬回证据所在地：
  建计划=提案（返回"待确认"+详情），批准=凭那张**限时 15 分钟、只能点一次、绑你身份**的确认单去执行；
- 画像视图把"已合并/已过期"的历史也当现状陈列 → 现在只讲**当下相信的**。

**生产实测（MCP 真调用）**：一条临时笔记 → 建计划（待确认）→ 在 Inbox 看见 → 批准 →
记录被安全下架、检索卡消失、对账作业成功；同一张确认单再点一次被拒（单次使用）。
健康检查回到 `failed_jobs: 0`，四条旧死信已标注清理。全套测试 **555 passed / 0 failed**。

**总账一句话：能通的路已经通到客户端了；不能替你走的那段（app 内部），我把每条路标都写清楚了。**