# 接续文档 · projects 通道修复收尾（2026-09-25 深夜）

> 给下一个会话/账号的完整交接。当前进度：**修复代码已全部写完并本地全绿，还差"提交最后一批改动 + 部署 + 清死信"三步收尾。**

---

## 一、任务背景（30 秒读懂）

手机端（Zafiro）反馈 projects 通道"只能建项目、其余全挂"。诊断确认四个根因并按用户拍板的**方案 B（一次修根）**修复：

1. `project.read` 从未进默认工具清单 → 6 个读类工具 TOOL_DENIED；
2. 建项目不给创建者发 `project:<id>` 作用域 → 自己的项目读写全 SCOPE_DENIED（孤儿项目）；
3. 删除计划缺 `review.write@projects` 授权 → 项目删不掉；
4. 无 list_projects → 建了什么永远发现不了。

## 二、已完成（均已验证）

| 事项 | 状态 |
|---|---|
| `dc88d70` 通道主体修复（默认清单+project.read、creator self-grant、list_projects 工具 32→33、删除墓碑兼容无 deleted_at 列的表、验收测试 test_projects_channel.py） | 已推送 GitHub + 已部署 prod + 真机验收全绿（A1~A7：建→读回→决策/约束可见→任务全流程→项目内检索→list_projects→治理删除） |
| `3c1063a` OAuth 包装器委托 self-grant（修"直连生效、MCP 不生效"的静默失效） | 已推送 + 已部署 + MCP 复测通过 |
| 数据面补票：zafiro/prod-trial 补 `project.read@projects`（含审计+epoch bump）、zafiro 补 `review.write@projects`、孤儿项目 `project-access write` | 已执行（走正规 CLI/脚本，有审计） |
| 孤儿项目清理：`62c343c3`（用户测试）、`bb369236`（诊断残留）、三个验收/诊断项目 | 全部经治理路径删除，`list_projects` 现存活项目 **0** |
| 新发现并修复的两个潜伏 bug（**未提交**，见下节）：① 索引器不认识 `decision:`/`constraint:`/`change_event:` 刷新作业 → 必死信（通道修好才首次触发）；② `create_project` 不拒绝空名称/空目的 → 空白文本索引死信 | 代码+测试已写，**本地全量 563 passed / 12 skipped** |

## 三、未提交的改动（下一步第一件事）

**属于本任务的（需要提交）：**
- `packages/infrastructure/personal_brain_infra/search/indexer.py`：新增 `_fact()`，支持 decision/constraint/change_event 三类刷新作业（scope=`project:<id>`，source_links 带事实与项目双链）
- `apps/server/personal_brain_server/api/authorized_tools.py`：`create_project` 边界校验空 name/purpose → VALIDATION_FAILED
- `tests/integration/test_projects_channel.py`：新增 2 个回归（事实刷新作业可索引、空名拒绝）

建议提交信息：`fix(projects): index fact refresh jobs and reject blank project creation`
（提交后 push，然后走部署，见第四节）

**属于用户并行会话的（不要动、不要提交）：**
- `packages/.../search/repository.py`（排序：长度归一化取代罚项，配对测试 `test_length_normalization.py`；`tests/unit/test_length_penalty.py` 已删）——**用户此前未拍板是否提交部署**，交接后先问
- `docs/acceptance/chain-audit-2026-09-24.md`、`docs/acceptance/real-journeys-2026-09-23/J*.jsonl`、`docs/ARCHITECTURE.md`、`docs/OPERATIONS.md`、`apps/.../bootstrap/settings.py`、`apps/worker/__main__.py`、`tests/migration/test_full_chain_postgresql.py`、`.agents/skills/mcp-personal-brain/SKILL.md`（被删）
- `deploy/windows-local/*` 调试脚本、`.execution/`、`个人信息.txt`

## 四、剩余步骤（按序执行）

1. **提交+推送**第三节"属于本任务"的三个文件。
2. **部署**：`deploy/windows-local/server_exec.py` 跑：
   `cd /home/kms/personal-brain-v1-prod && git pull --ff-only && BRAIN_DB_PASSWORD_FILE=/home/kms/personal-brain-v1-prod-data/secrets/db_password BRAIN_DB_DSN_FILE=.../db_dsn BRAIN_TOKEN_PEPPER_FILE=.../token_pepper BRAIN_MODEL_API_KEY_FILE=.../model_api_key docker compose -f deploy/compose.prod.yaml up -d --build api worker`
   （四个 env 缺一不可；参考 `run_backfills.py` 的 paramiko 用法）
3. **清 4 条历史死信**（都是修复前的产物，作业引用的记录已删除，不可重试只能注销）：
   `refresh_project_context`×3（decision/constraint/change_event，指向已删项目的 facts）+ `bootstrap_project`×1（空名项目 bb369236）。
   用 `server_sql.py` 执行 `update jobs set state='cancelled', error_summary='修复前产物：目标记录已随项目治理删除，运维清理' where state='dead_letter';`
4. **终验**：`/doctor` 回 `jobs: healthy, overall: healthy`；MCP 冒烟：33 工具、`create_project`→`record_decision`→查 `refresh_project_context` 作业 `succeeded` 且卡片生成（scope=project:<id>）→`list_projects`→治理删除。凭据获取模式：`rotate-client --credential-file /tmp/trial.cred` → 脚本读 `/tmp/trial.cred` → 用完 `rm`（zafiro client_id=`102b8f90-5b0c-4657-80f2-c9b388c47a1b`）。
5. **补验收文档** `docs/acceptance/projects-channel-2026-09-25.md`（本文件第二节表格 + A1~A7 结果 + 两个新 bug 的证据），提交推送。
6. **待用户拍板的遗留**：① 排序长度归一化是否提交部署（影响：现在生产还是旧罚项规则，长档案检索会被压后）；② 手机端"主动性"四层方案（提示词/看提醒工具/归档脚本）只给过文字，未实施；③ 画像里可能还有 open 的 merge_candidate 等用户在手机上批。

## 五、关键操作事实（免得重新摸索）

- 本机测试命令：`$env:BRAIN_TEST_POSTGRES_DSN='postgresql+psycopg://brain:5f52a011...@127.0.0.1:55432/brain_test'; uv run pytest tests -q --ignore=tests/benchmark`（DSN 完整值见 `docs/acceptance/` 或历史命令；本机 PG 55432 当前已启动，若停了用 `E:\Personal-Brain-V1-local\postgres\Library\bin\pg_ctl.exe -D E:\Personal-Brain-V1-local\pgdata -l ...postgres.log -o "-h 127.0.0.1 -p 55432" start`）
- 生产：SSH `kms@192.168.10.7`（密码在 `server_exec.py` 内），代码 `/home/kms/personal-brain-v1-prod`，容器 `personal-brain-v1-prod-{api,worker,db,model-proxy}-1`，端口 18083，本机工具 `deploy/windows-local/server_exec.py`（单行命令）/`server_sql.py`（只读 SQL，stderr 里的 permission denied 是无害噪声）/`run_backfills.py`（paramiko+sftp+容器内脚本的标准范式）
- 长脚本传容器：`base64 -d | docker exec -i <容器> python -`，或参照 `C:\Users\槐至\AppData\Local\Temp\run_profile_import.py` 的 sftp+docker cp 范式（Windows 命令行长度限制，别把大 base64 塞命令行）
- MCP 调用细节（嵌套 JSON、headers、33 工具清单）见 `.agents/skills/mcp-personal-brain/SKILL.md`（注意：该文件在工作区显示被删，需要时从 git 恢复 `git checkout -- .agents/skills/mcp-personal-brain/SKILL.md`）
- 知识库现状：个人信息.txt 已按 19 章节导入生产（全带向量）；活画像 125 条；反思分类已入库（record `eaced526`）；生产死信清零后健康即恢复

## 六、给接班会话的提醒

- 用户偏好：白话+故事体交付、文档驱动、无授权不动手、真实环境证据留痕（docs/acceptance/）
- 严禁动第五节"用户并行会话"清单里的文件
- 额外发现未处理：`get_project_recovery` 不筛 `lifecycle_state`（已删项目仍可读回详情）——低优先级，可记入 backlog
