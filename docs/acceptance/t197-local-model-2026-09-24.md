# T197: Local Windows Model Activation — Evidence (2026-09-24)

> 依据：specs/002-mcp-fixes 特征 US3；快速验收见 specs/002-mcp-fixes/quickstart.md。
> 本文件**不含任何密钥值**；仅记录可复现的运行结果。

## 前置

- 密钥文件：`E:\Personal-Brain-V1-local\secrets\model-api-key`（36 字符 Ark API Key，仅本机运行时目录，已被仓库 `.gitignore` 的 `secrets/` 排除，且位于仓库根之外）
- 对话模型：`deepseek-v4.1-flash`（Base URL `https://ark.cn-beijing.volces.com/api/coding`）
- 向量模型：`doubao-embedding-vision` 1024 维（Base URL `https://ark.cn-beijing.volces.com/api/coding/v3`）
- `start.ps1` 检测存在的非空密钥文件 → 设置 `BRAIN_EXTERNAL_MODELS_ENABLED=true` 与 `BRAIN_MODEL_API_KEY_FILE`
- 服务端点：`http://127.0.0.1:18082/mcp`（MCP 2025-11-25）

## 1. 单元测试（T017）

`tests/unit/test_model_settings.py`（新增 3 项）：
- 无密钥时 `external_models_enabled=false` 且模型名 `disabled`（fail-closed 不变）
- 启用时必须提供密钥文件且仅允许认可 base_url
- 序列化 dump 不含密钥值

结果：`3 passed`。

## 2. 服务就绪（T021）

`POST /ready` 返回：
```json
{"ready": true, "database": "ok", "worker": "ok", "storage": "ok"}
```

## 3. 真实问答（T022a）— answer_brain

经本机 MCP 调用 `answer_brain("请用一句话总结你现在使用的对话模型名称，以及你基于什么证据回答。", requested_scope="knowledge")`：

返回真实模型生成（grounded）回答，示例核心内容：
> "…现有证据仅提到…没有任何模型名称信息。"
并附 `sources`（含 `raw_input:<id>` 溯源）、`freshness: fresh`、`grounded`。

判定：**非 fail-closed**；模型 gateway 真实调用 Anthropic 兼容协议成功；回答基于权限过滤后的已授权证据且诚实不臆测。

## 4. 向量检索（T022b / T022c）— search_brain + 真实 embedding

- 用真实 `doubao-embedding-vision` 为一条冒烟笔记生成 1024 维 embedding，经 `PostgresSearchRepository` 契约入库（`vector_model_version=volcengine:doubao-embedding-vision:1024`），`VECTOR_ROWS=1`。
- 经 MCP `search_brain(query="冒烟测试笔记", requested_scope="knowledge", sensitivity_ceiling="private")`：
  - `authority: hybrid`，`semantic_status: generated`（查询向量由本机真实生成）
  - 命中含 `vector_model_version=volcengine:doubao-embedding-vision:1024` 的向量行
  - 每个命中携带 `ranking_reasons`（如 `["freshness=fresh", "source=canonical"]`）

## 5. 密钥安全核验（T018/T027）

- `git grep -n "<key-前6字符>" $(git rev-list --all)` → **零命中**
- 本机日志 `E:\Personal-Brain-V1-local\*.log` → **零命中**
- 密钥值未出现在本文件与任何 spec/contract 文档中

## 6. 回归快照

US1/US3 改动后局部回归：`tests/unit tests/contract tests/integration tests/security` → **356 passed / 1 skipped，0 failed**（全量回归见 Polish 阶段 t197 收口）。

## 结论

T197（本机真实模型激活）完成：真实接地 `answer_brain`、真实向量生成与向量命中、密钥 fail-closed 语义保持、密钥零泄漏。本机与局域网实例保持独立数据域，本激活不影响局域网（192.168.10.7:18081）配置。

## 全量回归与追踪（specs/002-mcp-fixes · Polish）

- 全量套件：`uv run pytest -q` → **447 passed / 23 skipped / 0 failed**
  （基线 431；本特征新增 16 项测试：test_ranking_reasons 6、test_ranking_contract 4、test_model_settings 3、deployment_boundary 新增 2 + 演进 1）
- 关联功能已成证据：`search_brain` 命中携带 `ranking_reasons`（ER-03 接线）、
  `get_brain_context` 返回 `ranking_reasons`、compose 提供 `profiles:["extras"]`
  trilium 可选服务（FR-100）。
- 追踪：specs/002-mcp-fixes/traceability 附录登记特征 FR/SC → ER-03/FR-057/FR-100/T197。